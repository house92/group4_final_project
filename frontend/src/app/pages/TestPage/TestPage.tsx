import { Stack } from '@mui/material';
import { useParams } from 'react-router-dom';
import { BookReviewIndex, UserDetails } from 'app/components';
import { ChatGptDetails } from 'app/components';

export default function TestPage() {

    return (
            < ChatGptDetails />
    );
}
