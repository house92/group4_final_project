query GetBooksList($pageLimit: Int!, $startOffset: Int!) {

    listBooks(connectionBuilder: { edgesPerPage: $pageLimit, startOffset: $startOffset }) {
        edges {
            node {
                id
                coverImage
                title
                authors {
                    firstName
                    lastName
                 }
                publicationDate
            }
        }
    }

}
