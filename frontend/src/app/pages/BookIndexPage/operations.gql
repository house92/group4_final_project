query GetBooksList($first: Int!, $page: Int!) {

    listBooks(first: $first, page: $page ) {
        edges {
            node {
                id
                coverImage
                title
                authors {
                    firstName
                    lastName
                 }
                publicationDate
            }
        }
    }

}
