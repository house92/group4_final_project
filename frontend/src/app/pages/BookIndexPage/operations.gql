query GetBooksList($limit: Int!, $offset: Int!) {

    listBooks(limit: $limit, offset: $offset) {
        id
        coverImage
        title
        authors {
            firstName
            lastName
        }
        publicationDate

    }

}
